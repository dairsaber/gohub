package migrations

import (
    "database/sql"
    "{{AppName}}/app/models"
    "{{AppName}}/pkg/migrate"

    "gorm.io/gorm"
)

func init() {

    type {{StructNamePlural}} struct {
        models.BaseModel

       // FIXME add custom field
       FIXME()

        models.CommonTimestampsField
    }

    up := func(migrator gorm.Migrator, DB *sql.DB) {
        migrator.AutoMigrate(&{{StructNamePlural}}{})
    }

    down := func(migrator gorm.Migrator, DB *sql.DB) {
        migrator.DropTable(&{{StructNamePlural}}{})
    }

    migrate.Add("{{FileName}}", up, down)
}